# Optional. The public Robinhood Chain RPC is used when unset.
RPC_URL=https://rpc.mainnet.chain.robinhood.com
# Optional comma-separated fallbacks tried in order when the primary fails.
RPC_FALLBACK_URLS=
# Never put a private key here. The tool refuses to read one.
