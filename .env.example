# Leaderboards and rooms are the only server-backed feature, and they are optional.
# With these unset the app builds and runs fully — leaderboard screens report
# "unavailable" instead of failing, and nothing is ever uploaded.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
